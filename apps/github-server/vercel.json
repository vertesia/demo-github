{
    "buildCommand": "echo 'No build command specified' && pwd && cd ../../out/apps/github-server && pnpm install",
    "installCommand": "cd ../.. && turbo prune --scope=@dglabs/github-server && cd out && pnpm install",
    "outputDirectory": "."
}
