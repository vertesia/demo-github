{
    "buildCommand": "echo \"[build] the current directory is $(pwd)\" && cd ../../out/apps/github-server && pnpm install && echo '[build] custom build command completed.'",
    "installCommand": "cd ../.. && pnpm vercel:install",
    "outputDirectory": "."
}
