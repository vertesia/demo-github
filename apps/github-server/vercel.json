{
    "buildCommand": "echo \"[build] the current directory is $(pwd)\" && pnpm build && echo '[build] custom build command completed.'",
    "ignoreCommand": "if [[ \"$VERCEL_GIT_COMMIT_REF\" == \"main\" ]]; then exit 1; else exit 0; fi",
    "installCommand": "cd ../.. && pnpm vercel:install",
    "outputDirectory": "."
}
