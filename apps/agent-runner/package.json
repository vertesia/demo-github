{
  "name": "@becomposable/agent-runner",
  "version": "1.0.0",
  "private": true,
  "description": "Agent Runner which runs an AI agent in the Composable Platform.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@becomposable/github-agent": "workspace:^"
  }
}
