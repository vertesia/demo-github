{
  "name": "@becomposable/simple-app",
  "version": "1.0.0",
  "private": true,
  "description": "Simple app for the GitHub demo.",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir -p dist && cp src/index.js dist/"
  }
}
