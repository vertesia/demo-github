{
  "name": "@dglabs/demo-github-root",
  "version": "1.0.0",
  "private": true,
  "description": "This is the root of the monorepo for the GitHub demo.",
  "scripts": {
    "agent:build": "docker buildx build --no-cache --platform linux/amd64 -t vertesia/github-agent:latest -f apps/github-agent/Dockerfile .",
    "agent:publish": "echo 'Publishing agent'"
  },
  "packageManager": "pnpm@9.15.0",
  "devDependencies": {
    "google-artifactregistry-auth": "^3.2.0",
    "vitest": "^2.1.8"
  }
}
